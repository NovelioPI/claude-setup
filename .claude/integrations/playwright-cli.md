# Playwright CLI

Prefer Playwright CLI + Skills for coding-agent browser work.

Why:
- lower context overhead than loading a large MCP tool surface;
- concise command-oriented interaction;
- easier to keep browser work inside a specialized Skill.

Use MCP instead only when persistent browser state or richer tool introspection is actually needed.
