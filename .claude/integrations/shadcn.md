# shadcn MCP

Use only in projects that already use the shadcn registry model.

Current setup can be initialized with:

```bash
npx shadcn@latest mcp init --client claude
```

The server can browse/search/install registry items. Prefer it for component discovery and reuse; do not load it for backend-only tasks.
